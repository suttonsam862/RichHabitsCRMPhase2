# Rich Habits CRM Environment Variables
# Phase 0 SEC-3: Properly secured environment configuration

# Node Environment
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://user:password@host:port/database?schema=public

# CORS Origins (comma-separated)
ORIGINS=http://localhost:5173

# Supabase Configuration (Required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT Secret (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-32-character-minimum-secret-key-here

# Optional Services
OPENAI_API_KEY=sk-...
SENDGRID_API_KEY=SG....
SENDGRID_FROM=no-reply@example.com
SENDGRID_FROM_NAME=Rich Habits CRM
APP_PUBLIC_URL=http://localhost:5173

# Feature Flags
ENABLE_DOMAIN_STUBS=0

# Security Flags (Phase 0)
# NEVER set these to true in production without explicit approval
ALLOW_ADMIN_SEED=false        # Allows admin user creation endpoint
ALLOW_DEBUG_ENDPOINTS=false   # Allows debug/diagnostic endpoints

# Object Storage (if using Replit Object Storage)
PUBLIC_OBJECT_SEARCH_PATHS=/bucket-name/public
PRIVATE_OBJECT_DIR=/bucket-name/private

# Monitoring (optional)
SENTRY_DSN=https://...@sentry.io/...